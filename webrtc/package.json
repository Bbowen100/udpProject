{
  "dependencies": {
    "node-turn": "^0.0.6"
  },
  "devDependencies": {
    "mediasoup-client": "^3.18.3",
    "socket.io-client": "^4.8.3",
    "vite": "^6.0.7"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "watch": "vite build --watch",
    "preview": "vite preview"
  }
}